Git Bash 

# 주요 command 

git add upload test.py

git commit -m "first commit"

# 처음 한번만 설정하면 됨.
git branch -M main

# 처음 한번만 설정하면 됨.
git remote add origin [본인_Git_repository주소].git

git push -u origin main